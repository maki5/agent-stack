{
  "name": "agent-stack",
  "version": "0.1.0",
  "description": "Reusable OpenCode agent workflow setup for any tech stack",
  "type": "module",
  "bin": {
    "agent-stack": "./bin/agent-stack.ts"
  },
  "scripts": {
    "start": "bun run bin/agent-stack.ts",
    "dev": "bun run --watch bin/agent-stack.ts"
  },
  "dependencies": {
    "kleur": "^4.1.5"
  },
  "devDependencies": {
    "bun-types": "latest"
  }
}
